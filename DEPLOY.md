# 🚀 Автоматический деплой Lavka26

## 📋 Что настроено

Проект готов к автоматическому деплою на **Render** с помощью **GitHub Actions**.

## 🔧 Требуется настройка

### 1. Переменные окружения в GitHub Secrets

Зайдите в ваш репозиторий GitHub → Settings → Secrets and variables → Actions и добавьте:

```
RENDER_API_KEY=your_render_api_key
RENDER_BACKEND_ID=your_backend_service_id
RENDER_FRONTEND_ID=your_frontend_service_id
RENDER_BOT_ID=your_bot_service_id
```

### 2. Как получить API ключ Render

1. Зайдите в [Render Dashboard](https://dashboard.render.com)
2. Settings → API Keys → Create API Key
3. Скопируйте ключ и добавьте в GitHub Secrets

### 3. Как получить Service ID

1. Зайдите в ваш сервис на Render
2. В URL адресе будет что-то вроде: `https://dashboard.render.com/web/srv-abc123def456/overview`
3. `srv-abc123def456` - это ваш Service ID
4. Добавьте его в GitHub Secrets

## 🔄 Как работает автоматический деплой

### Автоматический триггер:
- При каждом `push` в ветку `main`
- При ручном запуске через Actions → Deploy to Render → Run workflow

### Процесс деплоя:
1. **Тестирование** - проверка кода во всех трех сервисах
2. **Деплой Backend** - API сервер с базой данных
3. **Деплой Frontend** - Mini App интерфейс  
4. **Деплой Bot** - Telegram бот

## 📱 Структура деплоя

```
lavka26-bot/
├── backend/     → lavka26-backend-rmr1.onrender.com
├── frontend/    → lavka26-frontend-rmr1.onrender.com  
├── bot/         → lavka26-bot-rmr1.onrender.com
└── render.yaml  → Конфигурация Render
```

## 🛠️ Конфигурация Render

Все сервисы уже настроены в `render.yaml`:

- **Backend**: Node.js, порт 3001, SQLite база данных
- **Frontend**: Node.js, порт 3000, статические файлы
- **Bot**: Node.js, Telegram Bot API интеграция

## 🔍 Проверка деплоя

После успешного деплоя проверьте:

1. **Backend**: `https://lavka26-backend-rmr1.onrender.com/health`
2. **Frontend**: `https://lavka26-frontend-rmr1.onrender.com/`
3. **Bot**: Отправьте `/start` вашему боту в Telegram

## 🚨 Возможные проблемы

### Если деплой не работает:
1. Проверьте правильность API ключа и Service ID в GitHub Secrets
2. Убедитесь что репозиторий подключен к Render
3. Проверьте логи в Actions вкладке GitHub

### Если бот не отвечает:
1. Проверьте `TELEGRAM_BOT_TOKEN` в настройках бота на Render
2. Убедитесь что webhook настроен правильно
3. Проверьте логи бота на Render

## 📊 Мониторинг

- **GitHub Actions**: Отслеживайте процесс деплоя
- **Render Dashboard**: Мониторьте состояние сервисов
- **Logs**: Смотрите логи каждого сервиса для отладки

## 🎯 Готово к использованию

После настройки переменных окружения ваш проект будет автоматически деплоиться при каждом обновлении кода!
