# 🚀 Запуск бота Lavka26

## 📋 Что нужно сделать для запуска:

### 1. Установить Python

Если Python не установлен:

1. **Скачайте Python:**
   - Перейдите на [python.org](https://www.python.org/downloads/)
   - Скачайте Python 3.9+ (рекомендуется 3.11)
   - При установке ОБЯЗАТЕЛЬНО поставьте галочку "Add Python to PATH"

2. **Проверьте установку:**
   ```cmd
   python --version
   pip --version
   ```

### 2. Установить зависимости

Откройте командную строку (cmd) или PowerShell в папке проекта:

```cmd
cd c:\Users\dudki\lavka26-bot
pip install -r requirements.txt
```

### 3. Настроить Supabase

1. **Создайте проект на [supabase.com](https://supabase.com)**
2. **В SQL Editor выполните скрипт `supabase_schema.sql`**
3. **Получите URL и ключ проекта в Settings → API**
4. **Обновите `.env` файл:**
   ```env
   SUPABASE_URL=https://ваш-url.supabase.co
   SUPABASE_KEY=ваш-anon-ключ
   ```

### 4. Проверить конфигурацию

```cmd
python test_config.py
```

Должно появиться:
```
🎉 Конфигурация корректна! Бот готов к запуску.
```

### 5. Запустить бота

```cmd
python run.py
```

Или:

```cmd
python bot.py
```

## 🔧 Если возникли проблемы:

### "python не является внутренней или внешней командой"
- Переустановите Python с галочкой "Add Python to PATH"
- Перезапустите командную строку

### "ModuleNotFoundError: No module named 'telegram'"
- Установите зависимости: `pip install -r requirements.txt`

### "Ошибка подключения к Supabase"
- Проверьте URL и ключ в `.env` файле
- Убедитесь, что SQL скрипт выполнен в Supabase

### "Токен бота невалидный"
- Проверьте токен в `.env` файле
- Убедитесь, что бот создан через @BotFather

## 📱 Проверка работы

1. Найдите бота в Telegram по имени
2. Отправьте `/start`
3. Проверьте главное меню:
   - 📄 Смотреть объявления
   - ➕ Создать объявление
   - ❤️ Избранное
   - 🔍 Поиск
   - ⚙ Настройки

## 🎯 Готово!

После успешного запуска вы увидите:
```
🚀 Инициализация бота Lavka26...
✅ Подключение к базе данных успешно
✅ Категории успешно инициализированы
✅ Инициализация завершена
🚀 Запуск бота...
```

Бот будет работать и отвечать на команды пользователей!

## 📞 Поддержка

Если что-то не работает:
1. Проверьте все шаги выше
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию `.env` файла
4. Запустите `python test_config.py` для диагностики
