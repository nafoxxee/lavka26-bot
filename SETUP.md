# 🚀 Быстрая настройка Lavka26

## 📋 Что нужно сделать

### 1. Создать бота в Telegram
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен бота

### 2. Настроить Render
1. Зарегистрируйтесь на [Render](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте 3 Web Service:
   - **lavka26-backend** (из папки `backend`)
   - **lavka26-frontend** (из папки `frontend`) 
   - **lavka26-bot** (из папки `bot`)

### 3. Настроить переменные окружения

#### Для бота:
- `TELEGRAM_BOT_TOKEN`: ваш токен от @BotFather
- `BACKEND_URL`: URL вашего backend сервиса
- `TELEGRAM_WEBHOOK_URL`: URL вашего backend сервиса

#### Для backend:
- `PORT`: 3001

#### Для frontend:
- `PORT`: 3000

### 4. Настроить автоматический деплой

#### Получите API ключ Render:
1. Render Dashboard → Settings → API Keys
2. Создайте новый API ключ

#### Получите Service ID:
1. Зайдите в каждый сервис на Render
2. Скопируйте ID из URL (например: `srv-abc123def456`)

#### Добавьте в GitHub Secrets:
1. GitHub репозиторий → Settings → Secrets
2. Add repository secret:
   - `RENDER_API_KEY`: ваш API ключ
   - `RENDER_BACKEND_ID`: ID бэкенда
   - `RENDER_FRONTEND_ID`: ID фронтенда
   - `RENDER_BOT_ID`: ID бота

## 🎯 Готово!

Теперь при каждом `push` в main ветку будет автоматически происходить деплой!

## 📱 Как использовать

1. Откройте вашего бота в Telegram
2. Нажмите "Start" или отправьте `/start`
3. Пользуйтесь маркетплейсом как Авито!

## 🔗 Ссылки после деплоя

- **Backend**: `https://lavka26-backend-rmr1.onrender.com`
- **Frontend**: `https://lavka26-frontend-rmr1.onrender.com`
- **Bot**: через Telegram

## 🚨 Если что-то не работает

1. Проверьте логи в Render Dashboard
2. Убедитесь что все переменные окружения настроены
3. Проверьте GitHub Actions логи
