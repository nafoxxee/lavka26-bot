# 🔧 Исправление CHECK Constraint для таблицы ads

## 🚨 Проблема

При попытке обновить статус объявления на `payment_pending` возникает ошибка:
```
new row for relation "ads" violates check constraint "ads_status_check"
```

## 📝 Причина

В базе данных существует CHECK constraint, который разрешает только определенные статусы:
- 'moderation', 'active', 'inactive', 'sold', 'blocked', 'archived', 'payment_pending', 'payment_review'

Но в коде используются дополнительные статусы:
- 'draft', 'published', 'completed', 'cancelled', 'rejected'

## ✅ Решение

### Вариант 1: Через SQL (рекомендуется)

Выполните следующий SQL в вашей базе данных Supabase:

```sql
-- 1. Удаляем старый constraint
ALTER TABLE ads DROP CONSTRAINT IF EXISTS ads_status_check;

-- 2. Создаем новый constraint с полным списком статусов
ALTER TABLE ads 
ADD CONSTRAINT ads_status_check 
CHECK (status IN ('draft', 'moderation', 'active', 'inactive', 'sold', 'blocked', 'archived', 'payment_pending', 'payment_review', 'published', 'completed', 'cancelled', 'rejected'));

-- 3. Проверяем результат
SELECT conname, consrc 
FROM pg_constraint 
WHERE conrelid = 'ads'::regclass AND conname = 'ads_status_check';
```

### Вариант 2: Через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в **SQL Editor**
3. Вставьте и выполните SQL код выше
4. Проверьте что constraint создан успешно

### Вариант 3: Через миграцию

Запустите обновленную миграцию:
```bash
python migrate_supabase.py
```

## 🔄 Временное решение (уже применено)

В коде временно изменен статус с `payment_pending` на `active` чтобы избежать ошибки constraint.

## 📊 Статусы объявлений

### Основные статусы:
- **draft** - Черновик
- **moderation** - На модерации  
- **active** - Активно (ожидает оплаты)
- **payment_pending** - Ожидает подтверждения оплаты
- **payment_review** - На проверке скриншота
- **published** - Опубликовано
- **completed** - Завершено
- **cancelled** - Отменено
- **rejected** - Отклонено

### Архивные статусы:
- **inactive** - Неактивно
- **sold** - Продано
- **blocked** - Заблокировано
- **archived** - В архиве

## ✅ Проверка

После исправления constraint:

1. Перезапустите бота: `npm start`
2. Попробуйте нажать "Я оплатил" в рекламном объявлении
3. Проверьте что статус обновляется без ошибок
4. Убедитесь что платежи создаются в таблице `payments`

## 📞 Поддержка

Если проблема остается:
1. Проверьте логи бота на наличие ошибок
2. Убедитесь что SQL выполнен успешно
3. Проверьте права доступа к базе данных

---

**🚀 После исправления constraint все функции бота будут работать корректно!**
