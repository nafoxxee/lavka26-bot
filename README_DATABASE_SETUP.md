# Безопасная настройка базы данных Supabase

## 📋 Обзор

Созданы безопасные SQL-скрипты которые не повредят существующие данные при выполнении.

## 🗂️ Файлы

### Основные скрипты:
- `safe_complete_schema.sql` - Полная инициализация всей схемы
- `safe_settings_new.sql` - Создание/обновление таблицы настроек
- `safe_add_hide_username.sql` - Добавление поля hide_username
- `safe_add_contact_field.sql` - Добавление поля contact
- `safe_add_promo_fields.sql` - Добавление полей для рекламных объявлений

### Устаревшие (не использовать):
- `add_hide_username_field.sql` ❌
- `safe_settings_table.sql` ❌
- `add_contact_field.sql` ❌
- `add_promo_fields.sql` ❌

## 🚀 Инструкция по использованию

### Вариант 1: Новая установка (рекомендуется)
```sql
-- Выполнить в Supabase SQL Editor
-- Полностью создаст все необходимые таблицы
\i safe_complete_schema.sql
```

### Вариант 2: Обновление существующей базы
```sql
-- Проверяет и добавляет только недостающие поля/таблицы
\i safe_add_hide_username.sql
\i safe_settings_new.sql
\i safe_add_contact_field.sql
\i safe_add_promo_fields.sql
```

## ✅ Преимущества безопасных скриптов

1. **Проверка существования** - Не вызовет ошибок если таблицы уже есть
2. **Проверка полей** - Добавляет только отсутствующие колонки
3. **Сохранение данных** - Существующие данные остаются нетронутыми
4. **Индексы** - Автоматически создает необходимые индексы
5. **Обработка ошибок** - Informative сообщения о статусе операций

## 📊 Структура базы данных

### Таблица `users`
- `id` - Primary Key
- `telegram_id` - Уникальный ID пользователя Telegram
- `username`, `first_name`, `last_name` - Данные пользователя
- `created_at`, `updated_at` - Временные метки

### Таблица `categories`
- `id` - Primary Key
- `name` - Уникальное название категории

### Таблица `ads`
- `id` - Primary Key
- `user_id` - Ссылка на пользователя
- `category_id` - Ссылка на категорию
- `title`, `description` - Контент объявления
- `price` - Цена
- `photos`, `videos` - Массивы медиа
- `website` - Сайт или соцсети
- `location` - Геолокация (JSON)
- `location_text` - Текстовый адрес
- `contact` - Контактная информация
- `hide_username` - Скрытие username в рекламе
- `status` - moderation, active, archived
- `is_relevant` - Актуальность объявления
- `is_promo` - Флаг рекламного объявления
- `created_at`, `updated_at` - Временные метки

### Таблица `settings`
- `id` - Primary Key
- `sbp_phone` - Телефон для СБП
- `sbp_bank` - Банк для СБП

### Таблица `favorites`
- `id` - Primary Key
- `user_id`, `ad_id` - Ссылки на пользователя и объявление
- `UNIQUE` - Предотвращение дубликатов

### Таблица `chats`
- `id` - Primary Key
- `ad_id` - Ссылка на объявление
- `initiator_id`, `responder_id` - Участники чата

### Таблица `payments`
- `id` - Primary Key
- `ad_id` - Ссылка на объявление
- `amount` - Сумма платежа
- `status` - Статус платежа
- `payment_method` - Способ оплаты

## 🔧 Рекомендации

1. **Используйте `safe_complete_schema.sql` для новой установки**
2. **Для обновлений выполняйте скрипты по очереди**
3. **Всегда делайте backup перед изменениями**
4. **Проверяйте результаты выполнения через SELECT запросы**

## ⚠️ Важные замечания

- Все скрипты используют `DO $$` блоки для безопасности
- Проверяют существование через `information_schema`
- Создают индексы для оптимизации производительности
- Не удаляют и не изменяют существующие данные
