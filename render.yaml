services:
  - type: web
    name: lavka26-backend
    env: node
    plan: free
    rootDir: ./backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    healthCheckGracePeriod: 60
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production

  - type: web
    name: lavka26-frontend
    env: node
    plan: free
    rootDir: ./frontend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    healthCheckGracePeriod: 30
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production

  - type: web
    name: lavka26-bot
    env: node
    plan: free
    rootDir: ./bot
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    healthCheckGracePeriod: 60
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_WEBHOOK_URL
        value: https://lavka26-backend-rmr1.onrender.com
      - key: BACKEND_URL
        value: https://lavka26-backend-rmr1.onrender.com
      - key: NODE_ENV
        value: production
