# 🚀 Настройка Supabase для Lavka26 Bot

## 📋 Что нужно сделать:

### 1. Создать проект Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите через GitHub аккаунт
3. Нажмите "New Project"
4. Выберите организацию или создайте новую
5. Создайте проект:
   - **Project Name**: `lavka26-bot`
   - **Database Password**: создайте надежный пароль
   - **Region**: выберите ближайший регион (например, Frankfurt)

### 2. Настроить таблицы

1. В проекте перейдите в **SQL Editor**
2. Нажмите "New query"
3. Скопируйте и вставьте содержимое файла `supabase_schema.sql`
4. Нажмите "Run" для выполнения

### 3. Получить ключи доступа

1. Перейдите в **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://lgotcmpdfysztzhzvtun.supabase.co`)
   - **anon public** ключ (начинается с `eyJ...`)

### 4. Настроить RLS (Row Level Security)

1. Перейдите в **Authentication** → **Settings**
2. В разделе "Site URL" добавьте: `https://api.telegram.org`
3. В разделе "Redirect URLs" добавьте: `https://api.telegram.org/bot*`

### 5. Обновить .env файл

Убедитесь, что в вашем `.env` файле правильные данные:

```env
TELEGRAM_BOT_TOKEN=8500920411:AAH_lBSuXMpkDLDMs6IRpLhrTe0G9JTaKmg
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-key-here
ADMIN_ID=379036860
CHANNEL_ID=@lavka26city
PAYMENT_PROVIDER_TOKEN=  # если будете использовать платежи
```

### 6. Проверить подключение

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите бота:
   ```bash
   python run.py
   ```

3. В консоли должны появиться сообщения:
   ```
   🚀 Инициализация бота Lavka26...
   ✅ Подключение к базе данных успешно
   ✅ Категории успешно инициализированы
   ✅ Инициализация завершена
   🚀 Запуск бота...
   ```

## 🔧 Если возникли ошибки:

### Ошибка "Подключение к базе данных не удалось"
- Проверьте URL и ключ в `.env` файле
- Убедитесь, что проект Supabase активен
- Проверьте, что таблицы созданы через SQL Editor

### Ошибка "Категории не инициализированы"
- Убедитесь, что SQL скрипт выполнен без ошибок
- Проверьте наличие таблицы `categories` в Supabase

### Ошибка импорта supabase
- Установите зависимости: `pip install -r requirements.txt`
- Проверьте версию Python (должна быть 3.8+)

## 📱 Тестирование бота

1. Найдите вашего бота в Telegram по имени
2. Отправьте команду `/start`
3. Проверьте работу всех функций:
   - 📄 Смотреть объявления
   - ➕ Создать объявление
   - ❤️ Избранное
   - 🔍 Поиск
   - ⚙ Настройки

## 💡 Дополнительные настройки

### Для публикации в канал:
1. Создайте канал `@lavka26city`
2. Добавьте бота в канал как администратора
3. Убедитесь, что `CHANNEL_ID` в `.env` правильный

### Для платежей:
1. Получите токен платежей через [@BotFather](https://t.me/botfather)
2. Добавьте его в `PAYMENT_PROVIDER_TOKEN`
3. Настройте прием платежей в вашем Telegram аккаунте

## 🎯 Готово!

После выполнения этих шагов ваш бот Lavka26 будет полностью функционален и готов к использованию!
